server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/microdb
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
